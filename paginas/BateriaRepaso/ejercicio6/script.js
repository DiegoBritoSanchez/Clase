function ejercicio6() {
  let archivo;
  for (let index = 1; index < 7; index++) {
    archivo += document.write(
      "<h" + index + ">Encabeazdo de nivel " + index + "</h" + index + ">"
    );
  }
}
